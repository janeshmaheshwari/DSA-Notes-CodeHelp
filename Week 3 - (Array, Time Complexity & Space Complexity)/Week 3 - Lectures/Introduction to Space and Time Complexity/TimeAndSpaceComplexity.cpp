#include <iostream>
using namespace std;

int main()
{
/*
    Big O: Upper Bound
    Theta: Average Bound
    Omega: Lower Bound

Least Complex                                                       Most Complex
O(1), O(logN), O(Sqrt(N)), O(N), O(NlogN), O(N^2), O(N^3), O(2^N), O(N!), O(N^N)
*/
    return 0;
}